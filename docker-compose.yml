version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    # This loads your local .env file if you have one
    env_file:
      - .env